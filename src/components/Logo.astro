<a href="/" class="logo" aria-label="Side Software Co — Home">
  <svg
    class="logo-mark"
    viewBox="0 0 32 32"
    width="28"
    height="28"
    aria-hidden="true"
  >
    <rect width="32" height="32" rx="7" fill="var(--color-accent)" />
    <text
      x="16"
      y="22"
      text-anchor="middle"
      font-family="'Inter', system-ui, sans-serif"
      font-weight="700"
      font-size="15"
      fill="var(--color-bg)">SS</text
    >
  </svg>
  <span class="logo-text">Side Software</span>
</a>

<style>
  .logo {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    text-decoration: none;
    color: var(--color-text);
    transition: color var(--transition-fast);
  }

  .logo:hover {
    color: var(--color-accent);
    text-decoration: none;
  }

  .logo-mark {
    filter: drop-shadow(0 0 8px var(--color-glow));
  }

  .logo-text {
    font-weight: 600;
    font-size: 1.1rem;
    letter-spacing: -0.02em;
  }
</style>
