<section class="hero">
  <div class="hero-glow" aria-hidden="true"></div>
  <div class="container hero-content">
    <p class="section-label">// software studio</p>
    <h1>
      Side Software<span class="accent-dot">.</span>
    </h1>
    <p class="tagline">
      Affordable, production-grade software. We deliver outcomes that used to
      take a full engineering team, powered by disciplined AI workflows and
      DevOps automation.
    </p>
  </div>
</section>

<style>
  .hero {
    position: relative;
    padding: var(--space-4xl) 0;
    overflow: hidden;
  }

  /* Orbiting gradient blobs */
  .hero-glow {
    position: absolute;
    inset: -50%;
    background: radial-gradient(
        circle at 30% 40%,
        rgba(34, 211, 238, 0.07) 0%,
        transparent 50%
      ),
      radial-gradient(
        circle at 70% 60%,
        rgba(167, 139, 250, 0.06) 0%,
        transparent 50%
      );
    animation: orbit 25s linear infinite;
    pointer-events: none;
  }

  :global([data-theme='light']) .hero-glow {
    background: radial-gradient(
        circle at 30% 40%,
        rgba(8, 145, 178, 0.06) 0%,
        transparent 50%
      ),
      radial-gradient(
        circle at 70% 60%,
        rgba(124, 58, 237, 0.05) 0%,
        transparent 50%
      );
  }

  .hero-content {
    position: relative;
    z-index: 1;
  }

  .hero h1 {
    font-size: clamp(2.5rem, 6vw, 4rem);
    font-weight: 600;
    letter-spacing: -0.04em;
    line-height: 1.1;
    color: var(--color-text);
  }

  .accent-dot {
    color: var(--color-accent);
  }

  .tagline {
    font-size: clamp(1.05rem, 2vw, 1.2rem);
    color: var(--color-text-muted);
    max-width: 560px;
    margin-top: var(--space-lg);
    line-height: 1.7;
  }

  @media (max-width: 639px) {
    .hero {
      padding: var(--space-2xl) 0 var(--space-3xl);
    }
  }
</style>
